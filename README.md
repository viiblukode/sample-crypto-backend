
## Setting up Env ##

First to run `npm install` to install node module pacakges



For Database setup

- To run migration script:
    ` npx sequelize db:migrate `

- To undo migration script:
    ` npx sequelize db:migrate:undo `

- To run seeder script (optional):
    ` npx sequelize db:seed:all`

